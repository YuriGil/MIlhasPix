import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  headerBar: {
    height: 64,
    width: '100%',
    backgroundColor: '#1E90FF',
    justifyContent: 'center',
    elevation: 2,
  },
  headerInner: {
    maxWidth: 1216,
    width: '100%',
    paddingHorizontal: 16,
    flexDirection: 'row',
# truncated to fit execution buffer
